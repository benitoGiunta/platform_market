# Base de données (utilisé par le backend / Prisma -> backend/.env)
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/platform_market

# Backend (backend/.env)
PORT=3001

# Frontend (frontend/.env)
VITE_API_URL=http://localhost:3001
